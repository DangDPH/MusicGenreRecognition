# MusicGenreRecognition

rename genres_original to genres

go to terminal, install all libraries in requirements.txt: pip install -r requirements.txt

run preprocessing_audio.py first to prepare spectrogram samples(need 1 time only)

run train_model and wait till it done

run genre_recognition with the syntax: python genre_reecognition.py "audio_file_you_want_to_predict.wav"
